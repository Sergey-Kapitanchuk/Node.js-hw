const validateBody = require("./validateBody");
const isValidId = require("./isValidId");
const validFavoriteBody = require("./validFavoriteBody")

module.exports = {
    validateBody,
    isValidId,
    validFavoriteBody,
}